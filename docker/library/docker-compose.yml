services:
  calibre-web:
    image: lscr.io/linuxserver/calibre-web:latest
    container_name: calibre-web
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Almaty
      - CALIBRE_LIBRARY_PATH=/books
    volumes:
      - calibre-config:/config
      - books:/books
    ports:
      - 8083:8083
    restart: no

  webdav:
    image: rclone/rclone:latest
    container_name: moon-webdav
    user: "1000:1000"
    environment:
      - TZ=Asia/Almaty
    volumes:
      - sync:/data
    ports:
      - 8084:8084
    command: serve webdav /data --addr :8084 --user admin --pass admin123
    restart: no

volumes:
  sync:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /srv/encrypted/app/library/sync
  calibre-config:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /srv/encrypted/app/library/calibre-config
  books:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /srv/data/books
